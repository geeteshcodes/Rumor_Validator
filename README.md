# Rumor_Validator
Health rumor fact-checker built with Mistral AI, LangGraph, and ChromaDB — extracts atomic claims, clusters them by semantic similarity, validates against a RAG store with DuckDuckGo web fallback, and returns a structured verdict via a live Streamlit dashboard.
